#include<stdio.h>

int main() {

    int x, y, soma;

    printf("Digite a variavel X: ");
    scanf("%d", &x);
    printf("Digite a variavel Y: ");
    scanf("%d", &y);

    soma = x + y;

    printf("A soma e %d", soma);

    return 0;
}
